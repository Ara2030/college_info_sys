# Зависимости ИС колледжа (дипломный проект)
# Установка: pip install -r requirements.txt

Django==6.1
psycopg[binary]==3.2.9
Pillow>=10.0
lxml>=5.0
openpyxl>=3.1        # экспорт отчётов в Excel (модуль 2.6)
reportlab>=4.0       # экспорт отчётов в PDF (модуль 2.6)
